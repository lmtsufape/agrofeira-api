spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/ecofeira?sslmode=disable&preferQueryMode=simple
    username: ecofeira
    password: ecofeira123
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-init-sql: "SELECT 1"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080